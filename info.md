# Switch Timer

Switch Timer is a Home Assistant custom integration for timed entity control.

## Highlights

- Timed `on`, `off`, and `toggle` actions
- Automatic revert behavior
- Restart-safe persistence and recovery
- State sensor at `sensor.switch_timer_state`

Install the integration from HACS or copy `custom_components/switch_timer` into your Home Assistant configuration.
